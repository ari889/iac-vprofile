name: Dummy Action

on:
  push:
    branches:
      - main
      - stage
  pull_request:

jobs:
  dummy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello from GitHub Actions! This is a dummy workflow."

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls -la
